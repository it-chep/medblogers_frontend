version: '3.8'

services:
  next-app:
    build: .
    container_name: my-next-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped